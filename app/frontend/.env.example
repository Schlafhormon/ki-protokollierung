# Backend API URL
VITE_API_URL=http://localhost:8010

# Guardrail for legacy browser-driven LLM calls. Larger transcripts should use
# the backend pipeline so transcript text is not sent as a huge JSON request.
VITE_MAX_CLIENT_LLM_TEXT_CHARS=120000
